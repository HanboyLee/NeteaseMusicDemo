import styled from "@emotion/styled/macro";
import React from "react";
import { CubeGrid } from "better-react-spinkit";

const Loading = ({ styles = {} }) => {
    return <CubeGrid color="#EEEEEE" size={"100%"} />;
};

export default Loading;
