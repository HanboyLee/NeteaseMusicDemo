import styled from "@emotion/styled/macro";
import React from "react";

const NotFoundPage = () => {
    return <Continer>抱歉網頁不存在</Continer>;
};

const Continer = styled.h1`
    color: #f00;
`;
export default NotFoundPage;
